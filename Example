import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.jdbc.core.JdbcTemplate;
import org.springframework.jdbc.core.namedparam.NamedParameterJdbcTemplate;

import javax.sql.DataSource;
import java.util.concurrent.Executor;

import static org.junit.jupiter.api.Assertions.*;

@SpringBootTest(classes = AppConfig.class)
class AppConfigTest {

    @Autowired
    private DataSource dataSource;

    @Autowired
    private JdbcTemplate jdbcTemplate;

    @Autowired
    private NamedParameterJdbcTemplate guidbJdbcTemplate;

    @Autowired
    private DataSource reportingdb;

    @Autowired
    private JdbcTemplate jdbcTemplateReporting;

    @Autowired
    private NamedParameterJdbcTemplate reportingGuidbJdbcTemplate;

    @Autowired
    private Executor asyncExecutor;

    @Test
    void testDataSourceBeanCreation() {
        assertNotNull(dataSource);
        // Add more assertions based on your specific DataSource properties and behavior
    }

    @Test
    void testJdbcTemplateBeanCreation() {
        assertNotNull(jdbcTemplate);
        // Add more assertions based on your specific JdbcTemplate properties and behavior
    }

    @Test
    void testGuidbJdbcTemplateBeanCreation() {
        assertNotNull(guidbJdbcTemplate);
        // Add more assertions based on your specific NamedParameterJdbcTemplate properties and behavior
    }

    @Test
    void testReportingdbBeanCreation() {
        assertNotNull(reportingdb);
        // Add more assertions based on your specific DataSource properties and behavior for reportingdb
    }

    @Test
    void testJdbcTemplateReportingBeanCreation() {
        assertNotNull(jdbcTemplateReporting);
        // Add more assertions based on your specific JdbcTemplate properties and behavior for reportingdb
    }

    @Test
    void testReportingGuidbJdbcTemplateBeanCreation() {
        assertNotNull(reportingGuidbJdbcTemplate);
        // Add more assertions based on your specific NamedParameterJdbcTemplate properties and behavior for reportingdb
    }

    @Test
    void testAsyncExecutorBeanCreation() {
        assertNotNull(asyncExecutor);
        // Add more assertions based on your specific Executor properties and behavior
    }
}
